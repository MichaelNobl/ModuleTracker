<UserControl x:Class="ModuleTracker.Wpf.View.OpenPdfView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModuleTracker.Wpf.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Border BorderThickness="1" BorderBrush="Black">
                <Image Grid.Row="0" Source="{Binding ImageSource}" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
            </Border>
            <WrapPanel Grid.Row="1" HorizontalAlignment="Right">
                <Button Command="{Binding CloseCommand}" Content="Close" Style="{StaticResource ButtonStyle1}" Margin="0 5 5 5"/>
            </WrapPanel>
            
        </Grid>
            
        
        
    </Grid>
</UserControl>
