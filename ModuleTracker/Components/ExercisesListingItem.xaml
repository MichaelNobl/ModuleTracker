<UserControl x:Class="ModuleTracker.Wpf.Components.ExercisesListingItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
        <Grid Margin="0 10 5 10" VerticalAlignment="Center" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Exercise" FontSize="16" Margin="0 0 5 0"/>
            <TextBlock Grid.Column="1" FontSize="16" Text="{Binding ExerciseNumber}" TextAlignment="Left" HorizontalAlignment="Left"/>
            <CheckBox Grid.Column="2" IsChecked="{Binding ExerciseIsCompleted}" VerticalAlignment="Center"/>

        </Grid>
    
</UserControl>
