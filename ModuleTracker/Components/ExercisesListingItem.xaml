<UserControl x:Class="ModuleTracker.Wpf.Components.ExercisesListingItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModuleTracker.Wpf.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="Exercise" FontSize="16" Margin="0 0 5 0"/>
        <TextBlock Grid.Column="1" FontSize="16" Text="{Binding ExerciseNumber}" TextAlignment="Left" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        <CheckBox Grid.Column="2" VerticalAlignment="Center" IsChecked="{Binding ExerciseIsCompleted}"/>

    </Grid>
</UserControl>
